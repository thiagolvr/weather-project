import styled from 'styled-components';

const CityCardContainer = styled.button`
  font-family: "Poppins", sans-serif;
  background: none;
  border: none;

  color: #fff;

  cursor: pointer;

  font-weight: 200;
  text-align: center;
  font-size: 1.2rem;
  letter-spacing: 1px;
`;

export default CityCardContainer;
